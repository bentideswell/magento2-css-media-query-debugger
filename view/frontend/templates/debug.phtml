<?php
/**
 *
 */
$canDisplay = false;
$params = $block->getRequest()->getParams();

foreach ((array)$block->getData('trigger_param_names') as $paramName) {
    $canDisplay = $canDisplay || ($paramName && isset($params[$paramName]));
}

echo $canDisplay ? '<div id="cssmqd"></div>' : '';